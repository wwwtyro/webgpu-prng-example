{
  "name": "webgpu-prng-example",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --outDir docs --base /webgpu-prng-example/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.9",
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  }
}
